package datastructures.week5.day1;

public class AmazonSearchBar {
    /**
     *
     * 1. UI :
     *  - cover cross browser.
     *  - how design looks - position, alignment, color,
     *  - cove cross-platform web/ app/ IOT/ devices like android/ ios
     *  - copy/paste allowed
     *
     * 2. API coverage :
     *  - how to search api and read/manupilate response.
     *  - how search api before login or after login.
     *  - how authentication/authorization done using tokens/bearer tokens shared to access app.
     *
     * 3. Compatility :
     *  - how diff browsers with diff versions on diff platforms behave.
     *
     * 4. Usability :
     *  - is search allowed by uploading images or scanning images.
     *  - speak to search allowed.
     *  - tooltips available.
     *
     * 5. Security layer:
     *  - headers validation, http methods manupilations allowed.
     *
     * 6. Perfomance :
     *   - load sequentially
     *   - spike tetsing
     *   - how data is consistent.
     *
     * 7. Functional
     *  - Business flows coverage :
     *      * search returns expected items.
     *      * search based on prcies
     *      * search based relevance.
     *   - Field level validations :
     *      * data enumerations like if text allowed with lower/upper/numerals/special chars etc.
     *
     *
     *
     *
     */
}
